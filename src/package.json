{
  "name": "nativescript-activity-detection",
  "version": "0.1.3",
  "description": "NativeScript plugin to recognize user's activity (motion) like walking, running or in vehicle.",
  "repository": "https://github.com/HonkMe/nativescript-activity-detection",
  "main": "index",
  "nativescript": {
    "platforms": {
      "android": "2.3.0",
      "ios": "2.3.0"
    }
  },
  "scripts": {
    "link": "npm link && cd ../demo && npm link nativescript-activity-detection && cd ../src",
    "clean": "rm -rf node_modules && npm i && cd ../demo && rm -rf hooks node_modules platforms && npm i && cd ../src && npm run link",
    "android": "cd ../demo && tns run android --syncAllFiles",
    "ios": "cd ../demo && tns run ios --syncAllFiles"
  },
  "author": "Nicolae Surdu",
  "license": "MIT",
  "dependencies": {
    "nativescript-android-utils": "^1.0.2",
    "nativescript-theme-core": "^1.0.4"
  },
  "devDependencies": {
    "eslint": "^4.4.0",
    "eslint-plugin-promise": "^3.5.0"
  }
}
